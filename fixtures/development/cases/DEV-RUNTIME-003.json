{
  "$schema": "../../schema/case-manifest.schema.json",
  "schema_version": "runparity.fixture-case/v1",
  "case_id": "DEV-RUNTIME-003",
  "suite": "open-development-v1",
  "kind": "supported_positive",
  "title": "Runtime manager selects a different Node provenance",
  "summary": "The shell-visible Node and the Node used by the package-manager launcher come from different pre-existing runtime-manager locations.",
  "fixture_status": "verified",
  "gold_category": "RUNTIME_MANAGER_DRIFT",
  "expected_terminal_verdict": "VERIFIED_INTERVENTION",
  "platform": {
    "os": "linux",
    "arch": "x64",
    "libc": "glibc",
    "execution_context": "linux_isolated_experiment"
  },
  "proof_eligibility": {
    "eligible": true,
    "backend": "linux_rootless_oci",
    "reason": "The planned case changes one typed dimension inside the supported Linux isolation backend."
  },
  "oracle": {
    "type": "exit_code_and_stdout",
    "description": "The unchanged fixture assertion exits zero and emits the DEV-RUNTIME-003 success marker.",
    "frozen_across_arms": true,
    "success": {
      "exit_code": 0,
      "stdout_contains": "RUNPARITY_OK:dev-runtime-003",
      "stderr_contains": null,
      "artifact_path": null
    }
  },
  "allowed_typed_intervention": {
    "type": "runtime.select",
    "target": "node",
    "parameters": {
      "runtime_role": "package_manager_runtime",
      "preinstalled_only": true
    }
  },
  "expected_refusal": null,
  "source_evidence_ids": ["SRC-GH-PNPM-10608", "SRC-GH-NVM-651"],
  "scenario": {
    "failure_mode": "The package-manager child observes a different Node marker than the direct shell-visible Node.",
    "planned_setup": [
      "Provide two pre-installed runtime-manager slots with distinct adjacent runtime-role markers.",
      "Bind the package-manager launcher to the unintended runtime slot.",
      "Keep PATH unchanged during runtime.select."
    ],
    "planned_target_argv": ["pnpm", "run", "assert-runtime-provenance"],
    "expected_a_failure_signature": "RP_FIXTURE_RUNTIME_PROVENANCE_SPLIT"
  },
  "safety_expectations": {
    "network": "disabled",
    "host_writes": "fixture_workspace_only",
    "sensitive_flag_probe": false,
    "unicode_control_probe": false,
    "timeout_cleanup_probe": false,
    "hmac_digest_probe": true,
    "recognized_shim_probe": false
  },
  "implementation": {
    "runnable": true,
    "asset_root": "development/assets/DEV-RUNTIME-003",
    "missing_assets": [],
    "receipts": {
      "build": "receipts/build/DEV-RUNTIME-003.json",
      "backend_qualification": "receipts/backend/qemu-kvm-ubuntu-noble-rpvm-2026-08-22.json",
      "verification_ledger": "receipts/ledger/DEV-RUNTIME-003.json"
    },
    "verified_at": "2026-08-22T13:38:54Z"
  }
}
