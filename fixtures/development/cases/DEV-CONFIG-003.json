{
  "$schema": "../../schema/case-manifest.schema.json",
  "schema_version": "runparity.fixture-case/v1",
  "case_id": "DEV-CONFIG-003",
  "suite": "open-development-v1",
  "kind": "supported_positive",
  "title": "CLI config overrides environment config",
  "summary": "A single explicit npm CLI setting wins over the matching npm_config environment key while all other inputs remain fixed.",
  "fixture_status": "verified",
  "gold_category": "CONFIG_PRECEDENCE",
  "expected_terminal_verdict": "VERIFIED_INTERVENTION",
  "platform": {
    "os": "linux",
    "arch": "x64",
    "libc": "glibc",
    "execution_context": "linux_isolated_experiment"
  },
  "proof_eligibility": {
    "eligible": true,
    "backend": "linux_rootless_oci",
    "reason": "The planned case changes one typed dimension inside the supported Linux isolation backend."
  },
  "oracle": {
    "type": "exit_code_and_stdout",
    "description": "The unchanged fixture assertion exits zero and emits the DEV-CONFIG-003 success marker.",
    "frozen_across_arms": true,
    "success": {
      "exit_code": 0,
      "stdout_contains": "RUNPARITY_OK:dev-config-003",
      "stderr_contains": null,
      "artifact_path": null
    }
  },
  "allowed_typed_intervention": {
    "type": "config.set",
    "target": "npm.fund",
    "parameters": {
      "source": "cli",
      "key": "fund",
      "value_role": "expected_fixture_value"
    }
  },
  "expected_refusal": null,
  "source_evidence_ids": ["SRC-NPM-CONFIG-PRECEDENCE"],
  "scenario": {
    "failure_mode": "The highest-priority CLI config value contradicts the fixture's expected effective value.",
    "planned_setup": [
      "Set npm_config_fund=true in the isolated environment.",
      "Forward one contradictory --fund=false CLI config flag to the lifecycle assertion through the fixed A-arm argv.",
      "Ask a real npm config get invocation for the effective value; perform no package installation."
    ],
    "planned_target_argv": ["npm", "run", "fixture:assert-config", "--", "--fund=false"],
    "expected_a_failure_signature": "RP_FIXTURE_CLI_OVERRIDES_ENV_CONFIG"
  },
  "safety_expectations": {
    "network": "disabled",
    "host_writes": "fixture_workspace_only",
    "sensitive_flag_probe": false,
    "unicode_control_probe": false,
    "timeout_cleanup_probe": false,
    "hmac_digest_probe": true,
    "recognized_shim_probe": false
  },
  "implementation": {
    "runnable": true,
    "asset_root": "development/assets/DEV-CONFIG-003",
    "missing_assets": [],
    "receipts": {
      "build": "receipts/build/DEV-CONFIG-003.json",
      "backend_qualification": "receipts/backend/qemu-kvm-ubuntu-noble-rpvm-2026-08-22.json",
      "verification_ledger": "receipts/ledger/DEV-CONFIG-003.json"
    },
    "verified_at": "2026-08-22T13:40:02Z"
  }
}
